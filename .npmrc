# react-x11 (1.2.0) and @react-x11/components (0.1.0, peer react-x11@^2) are
# co-developed pre-release repos whose version numbers are out of sync. They
# work together at runtime; this lets `npm install` proceed past the declared
# peer gap. Remove once the packages publish aligned versions.
legacy-peer-deps=true
